import { InitiateWishListPipe } from './initiate-wish-list.pipe';

describe('InitiateWishListPipe', () => {
  it('create an instance', () => {
    const pipe = new InitiateWishListPipe();
    expect(pipe).toBeTruthy();
  });
});
